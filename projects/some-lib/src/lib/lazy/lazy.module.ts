import { NgModule } from '@angular/core';
import { LazyListingComponent } from './components/listing/listing.component';

@NgModule({
  declarations: [
    LazyListingComponent,
  ],
  imports: [
  ],
  exports: [LazyListingComponent]
})
export class LazyModule { }
